For start project: clone it, build images and run docker-compose
```
git clone https://github.com/CHESTERFIELD/url-shorter-jooble-test.git
cd url-shorter-jooble-test     
docker-compose up --build
```